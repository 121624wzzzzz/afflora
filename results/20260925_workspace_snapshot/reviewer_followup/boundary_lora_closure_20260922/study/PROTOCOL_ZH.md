# 收束验证：2026-09-22

依据REVIEW_ZH.md补缺口，不扩模型族。旧队列全部结束并完成证据核验后启动。旧初始adapter快照缺失单列留存限制；最终checkpoint/逐条输出/官方评分审计仍核验。

- 共享：Qwen3-0.6B、Qwen2.5-1.5B；单侧U：Qwen2.5-7B、Qwen3-8B、Llama3.1-8B。全部覆盖CLUENER/WikiSQL，不排除无优势条件。
- 每个模型任务，普通词表LoRA r1与aLoRA r16各4个边界LR倍率[1/16,1/4,1,4]×2个dev-only种子。总160次训练。固定H rank8/alpha16，H LR2e-4，其余沿用已审计协议。仍是有限搜索，不声称最优超参数。
- 按dev平均primary选配置；并列优先倍率距1更近，再较小倍率。选定后冻结，100次新确认：10模型任务×2方法×5种子。
- dev seeds CLUENER106100/106101，WikiSQL107100/107101；确认206100..206104/207100..207104。不使用新种子挑方法；test仍是历史test，不称独立新任务。
- 新确认50次同种子H-only，加旧确认批次40次H-only（6300..6304/7300..7304）。总90次。H LR固定，回答固定主干下的叠加收益，不声称最佳H对照。
- 共350次正式训练+4次资源记录代码短测。已验证的122个旧架构短测门控核验后复用。运行代码只新增资源测量，不改变loss/优化器/生成/评分；4次短测通过后再放行正式训练。
- 主比较10个模型任务的aLoRA−普通LoRA，另10个aLoRA−H；每组五种子配对差异、95% t区间、完整比较族Holm校正。普通LoRA−H也报告。不以不显著等同于非劣或等效。
- 旧确认另8个配对H比较单独报告；共同LR与调参结果不混合均值。
- 资源测量包含训练循环同步耗时、峰值allocated/reserved显存和实际adapter字节。另对两个小模型H/共享aLoRA16/共享普通LoRA1做同卡顺序微基准，3轮轮换顺序，固定2条CLUENER训练样本，warmup2步+计时10步，固定32新token生成；验证边界合并logits一致后测合并生成。H保持同一架构。共享机器争用限制明确披露，不把该测量泛化为所有batch/硬件收益。
- 磁盘预估42.2GiB完整双份checkpoint，剩余低于20GiB不再发新任务，不删除旧文件。所有新初始和最终checkpoint保留。
